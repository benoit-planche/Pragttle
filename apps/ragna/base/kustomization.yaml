apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: ragna-base

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml

commonLabels:
  app: ragna
  version: v1.0.0 